with tyr.math.`3d`.Vector

class Entity {

    var position : Vector
    val id : int

    new () {
        id = EntityManager.register(this)
    }

    def proxy : entityProxy = new entityProxy(id)
}